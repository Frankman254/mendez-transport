// ─── MENDEZ TRANSPORT — CONFIGURABLE PRICES ────────────────────────────────
// Edit the `price` values here to update all pages simultaneously.
// Keys must match the destination slugs used across the app.

export const PRICES = {
  bocasDelToro:  65,
  elValle:       40,
  panamaCity:    55,
  boquete:       35,
  david:         30,
  playaVenado:   40,
};
